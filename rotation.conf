# Session rotation pattern.
# Each letter = one session type: E=engage, R=reflect, B=build
# The pattern repeats. Edit this to change the rotation.
# Example: EEBER = 3 engage, 1 build, 1 reflect per cycle
PATTERN=BEBLR
